export type ProjectScreenType = {
    id: number;
}

export type ProjectDetailScreenType = {
    id: number;
}

export type CreateScreenType = {
    id: number;
}

export type ActivityScreenType = {
    id: number;
    type: number,
}